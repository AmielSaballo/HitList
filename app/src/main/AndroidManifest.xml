<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobcomfinals">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListModifyScheme"
            android:label="@string/edit_scheme">
        </activity>
        <activity android:name=".ListModifyMilestone"
            android:label="@string/edit_milestone" />
        <activity android:name=".ListModifyTask"
            android:label="@string/edit_task"/>
        <activity android:name=".AddTaskActivity"
            android:label="@string/add_task"/>
        <activity android:name=".AddMilestoneActivity"
            android:label="@string/add_milestone"/>
        <activity android:name=".ModifyTaskActivity"
            android:label="@string/edit_task"/>
        <activity android:name=".ModifyMilestoneActivity"
            android:label="@string/edit_milestone"/>
        <activity android:name=".ViewTaskActivity"
            android:label="@string/app_task"/>
        <activity android:name=".ModifySchemeActivity"
            android:label="@string/edit_scheme"/>
        <activity android:name=".AddSchemeActivity"
            android:label="@string/add_scheme"/>
        <activity android:name=".ViewMilestoneActivity"
            android:label="@string/app_milestone"/>
        <activity android:name=".DBManager" />
        <activity android:name=".ViewHitListActivity"
            android:label="@string/app_scheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>